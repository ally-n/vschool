import React, { useState } from "react"
const ThemeContext = React.createContext()

//provider is the root of the data- state/methods
//consumer is wherever the data needs to be passed

function UglyThingsContext(props) {
    
    return (
        <div>

        </div>
    )

}

export default UglyThingsContext